# Supabase (frontend — anon key only)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-side (Vercel /api/* only — never expose to client)
SUPABASE_SERVICE_ROLE_KEY=
ANTHROPIC_API_KEY=
RESEND_API_KEY=
TELEGRAM_BOT_TOKEN=
